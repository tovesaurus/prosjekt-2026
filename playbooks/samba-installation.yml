---
# This playbook installs Samba and related packages required for AD integration

- name: Install Samba and dependencies for AD integration
  hosts: samba_fileshare
  become: true

  tasks:
    - name: Install Samba, winbind, Kerberos and libnss-winbind
      ansible.builtin.apt:
        name:
          - samba
          - winbind
          - libnss-winbind
          - krb5-user
        state: present
        update_cache: yes