---
- name: Run playbook on itself
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Install iptables-persistent
      ansible.builtin.apt:
        name: iptables-persistent
        state: present
        update_cache: true
