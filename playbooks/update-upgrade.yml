---
# Playbook that updates package list, upgrades installed packages, and removes unused packages and cache
- name: Update and Upgrade packages
  hosts: all
  become: true

  tasks:
    - name: Update package list
      ansible.builtin.apt: 
        update_cache: yes 
        force_apt_get: yes 
        cache_valid_time: 3600
    
    - name: Upgrade installed packages
      ansible.builtin.apt: 
        upgrade: dist
        force_apt_get: yes

    - name: Remove unused packages and cache
      ansible.builtin.apt:
        autoclean: yes
        autoremove: yes

# Forbedringer til vurdering: reboot hvis det er nødvendig noen form for logging.

    # - name: Check if reboot is required
    #   register: reboot_required_file
    #   stat: path=/run/reboot-required

    # - name: Reboot servers if there was a kernel Update
    #   reboot: 
    #     msg: "Rebooting servers because kernel was updated"
    #     connection_timeout: 5
    #     reboot_timeout: 300
    #     pre_reboot_delay: 30
    #     post_reboot_delay: 30
    #     test_command: uptime
    #   when: reboot_required_file.stat.exists